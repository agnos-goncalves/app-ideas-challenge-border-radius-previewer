describe('First Test', () => {
  beforeEach(() => {
    cy.visit('http://localhost:8080');
  });
  it('When adding the first input value should apply the same value to all input elements and render style border instruction', () => {});
});
